get:
  summary: サブネットの詳細取得
  description: 指定した仮想ネットワーク内のサブネットの詳細情報を取得します。
  tags:
    - VirtualNetwork
  parameters:
    - name: networkId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: 仮想ネットワークの一意なID
      example: "12fbed88-2dc4-4537-af98-f1f796046d16"
    - name: subnetId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: サブネットの一意なID
      example: "12fbed88-2dc4-4537-af98-f1f796046d16"
  responses:
    "200":
      description: 成功
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/virtual-network/SubnetResponse.yml"
    "400":
      description: リクエストエラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "401":
      description: 認証エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "403":
      description: 権限エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "404":
      description: 仮想ネットワークまたはサブネットが見つからない
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
  security:
    - BearerAuth: []
put:
  summary: サブネットの更新
  description: 指定した仮想ネットワーク内のサブネット情報を更新します。
  tags:
    - VirtualNetwork
  parameters:
    - name: networkId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: 仮想ネットワークの一意なID
      example: "12fbed88-2dc4-4537-af98-f1f796046d16"
    - name: subnetId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: サブネットの一意なID
      example: "12fbed88-2dc4-4537-af98-f1f796046d16"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/virtual-network/SubnetPutRequest.yml"
  responses:
    "200":
      description: 更新成功
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/virtual-network/SubnetResponse.yml"
    "400":
      description: リクエストエラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "401":
      description: 認証エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "403":
      description: 権限エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "404":
      description: 仮想ネットワークまたはサブネットが見つからない
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
  security:
    - BearerAuth: []
patch:
  summary: サブネットの部分更新
  description: 指定した仮想ネットワーク内のサブネット情報を更新します。
  tags:
    - VirtualNetwork
  parameters:
    - name: networkId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: 仮想ネットワークの一意なID
      example: "12fbed88-2dc4-4537-af98-f1f796046d16"
    - name: subnetId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: サブネットの一意なID
      example: "12fbed88-2dc4-4537-af98-f1f796046d16"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/virtual-network/SubnetPatchRequest.yml"
  responses:
    "200":
      description: 更新成功
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/virtual-network/SubnetResponse.yml"
    "400":
      description: リクエストエラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "401":
      description: 認証エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "403":
      description: 権限エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "404":
      description: 仮想ネットワークまたはサブネットが見つからない
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
  security:
    - BearerAuth: []
delete:
  summary: サブネットの削除
  description: 指定した仮想ネットワーク内のサブネットを削除します。
  tags:
    - VirtualNetwork
  parameters:
    - name: networkId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: 仮想ネットワークの一意なID
      example: "12fbed88-2dc4-4537-af98-f1f796046d16"
    - name: subnetId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: サブネットの一意なID
      example: "12fbed88-2dc4-4537-af98-f1f796046d16"
  responses:
    "204":
      description: 削除成功
    "400":
      description: リクエストエラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "401":
      description: 認証エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "403":
      description: 権限エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "404":
      description: 仮想ネットワークまたはサブネットが見つからない
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
  security:
    - BearerAuth: []
