get:
  summary: 仮想ネットワーク一覧の取得
  description: すべての仮想ネットワークのリストを取得します。
  tags:
    - VirtualNetwork
  responses:
    "200":
      description: 成功
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../components/schemas/virtual-network/VirtualNetwork.yml"
    "401":
      description: 認証エラー
    "403":
      description: 権限エラー
  security:
    - BearerAuth: []
post:
  summary: 仮想ネットワークの作成
  description: 新しい仮想ネットワークを作成します。
  tags:
    - VirtualNetwork
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/virtual-network/VirtualNetworkCreateRequest.yml"
  responses:
    "201":
      description: 作成成功
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/virtual-network/VirtualNetwork.yml"
    "400":
      description: リクエストエラー
    "401":
      description: 認証エラー
    "403":
      description: 権限エラー
  security:
    - BearerAuth: []
