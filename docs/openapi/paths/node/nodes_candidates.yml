get:
  summary: 物理ノード候補一覧の取得
  description: 物理ノードとして追加可能な候補のリストを取得します。
  tags:
    - Node
  responses:
    "200":
      description: 成功
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../../components/schemas/node/NodeCandidateResponse.yml
    "401":
      description: 認証エラー
    "403":
      description: 権限エラー
  security:
    - BearerAuth: []
