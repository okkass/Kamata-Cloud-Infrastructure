get:
  summary: ユーザの詳細取得
  description: 指定したIDのユーザの詳細情報を取得します。
  tags:
    - User
  parameters:
    - name: userId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: ユーザの一意なID
  responses:
    "200":
      description: 成功
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/user/UserResponse.yml"
    "401":
      description: 認証エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "403":
      description: 権限エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "404":
      description: ユーザが見つからない
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
  security:
    - BearerAuth: []
put:
  summary: ユーザの更新
  description: 指定したIDのユーザ情報を更新します。
  tags:
    - User
  parameters:
    - name: userId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: ユーザの一意なID
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/user/UserPutRequest.yml"
  responses:
    "200":
      description: 更新成功
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/user/UserResponse.yml"
    "400":
      description: リクエストエラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "401":
      description: 認証エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "403":
      description: 権限エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "404":
      description: ユーザが見つからない
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
  security:
    - BearerAuth: []
patch:
  summary: ユーザの部分更新
  description: 指定したIDのユーザ情報を部分的に更新します。
  tags:
    - User
  parameters:
    - name: userId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: ユーザの一意なID
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/user/UserPatchRequest.yml"
  responses:
    "200":
      description: 更新成功
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/user/UserResponse.yml"
    "400":
      description: リクエストエラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "401":
      description: 認証エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "403":
      description: 権限エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "404":
      description: ユーザが見つからない
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
  security:
    - BearerAuth: []
delete:
  summary: ユーザの削除
  description: 指定したIDのユーザを削除します。
  tags:
    - User
  parameters:
    - name: userId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: ユーザの一意なID
  responses:
    "204":
      description: 削除成功
    "401":
      description: 認証エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "403":
      description: 権限エラー
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
    "404":
      description: ユーザが見つからない
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yml"
  security:
    - BearerAuth: []
