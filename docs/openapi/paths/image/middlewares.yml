get:
  summary: ミドルウェア一覧の取得
  description: すべてのミドルウェアのリストを取得します。
  tags:
    - Image
  responses:
    "200":
      description: 成功
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../../components/schemas/image/Middleware.yml
    "401":
      description: 認証エラー
    "403":
      description: 権限エラー
  security:
    - BearerAuth: []
