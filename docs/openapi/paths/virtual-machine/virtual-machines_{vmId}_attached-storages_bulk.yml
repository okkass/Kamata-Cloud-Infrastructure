post:
  summary: 仮想マシンのストレージ一括編集
  description: 指定したIDの仮想マシンにストレージを一括で編集します。
  tags:
    - VirtualMachine
  parameters:
    - name: vmId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: 仮想マシンの一意なID
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../../components/schemas/virtual-machine/virtual-storage/VirtualStorageBulkRequest.yml
