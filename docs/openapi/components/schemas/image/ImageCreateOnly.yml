type: object
description: イメージ作成時のみ設定可能なプロパティ
properties:
  nodeId:
    type: string
    format: uuid
    description: イメージを作成するノードのID
  file:
    type: string
    format: binary
    description: アップロードするイメージファイル
required:
  - nodeId
  - file