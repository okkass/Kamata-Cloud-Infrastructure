type: object
description: アタッチされたストレージオブジェクト
properties:
  id:
    type: string
    format: uuid
    description: アタッチされたストレージを識別するための一意なID
  storage:
    $ref: ./VirtualStorage.yml
  path:
    type: string
    description: ストレージデバイスのパス
    example: /dev/sda
required:
  - id
  - storage
  - path
