type: object
description: 仮想マシン作成時のみに設定可能なプロパティを持つオブジェクト
properties:
  publicKey:
    type: string
    description: 仮想マシンに設定するSSH公開鍵
  middlewareId:
    type: string
    format: uuid
    description: インストールするミドルウェアのID
required:
  - publicKey
